// pertenecen todos las variables que contiene la palabra const, let
// solo podemos acceder a ellas en un mismo bloque de codigo

 const edad = 19;

 if (edad >= 18) {
    const accesoPermitido = true;
    console.log(accesoPermitido)


 };
  
 // los bloques son las {}
